# 変更履歴

変更を入れたら `package.json` の `version` を更新し、このファイルの先頭に節を追加する（運用ルールは `AGENTS.md` の「バージョン管理」を参照）。

## [6.0.0] - 2026-08-29

### 改善
- **アプリ構成をタブナビゲーションに刷新**: 縦スクロール1本だったダッシュボードを、目的ごとの4タブに分割した (#83)
  - **ホーム / 履歴 / 振り返り / 設定**。モバイルは画面下部の固定バー、デスクトップはヘッダー直下の横並びタブ
  - タブごとに URL を持つ（`/`・`/history`・`/review`・`/settings`）。ブラウザの戻るが効き、`/history?month=2025-03` のように直接開ける。ルートグループ `(tabs)` を使うため**既存の URL は変わらない**
  - 認証ガードとローディング待ちを `(tabs)/layout.tsx` に集約。3ページに同じ処理が重複していたのを解消した
- **履歴タブを新設**: 取引リストとカレンダーを1つの画面にまとめ、上部で切り替える。どちらも「取引を見る」同じ目的なので、探す場所を2つに分けない
- **カレンダーをページに埋め込めるようにした**: `MobileCalendar` から `CalendarView` を切り出し、モーダル版は取引フォームの日付選択用に残した。あわせてカレンダーの配色を Quiet Ledger のトークンに揃えた（ダークモードで枠線が明るいままだったのを修正）
- **ホームを「今月」に絞った**: 取引リストとカレンダーを履歴タブへ移し、ヘッダーの遷移アイコンはタブバーに置き換えた

### 追加
- `--tabbar-height` を追加し、タブバーの高さ・取引追加ボタンの位置・本文の下余白が同じ値を参照するようにした
- 月ナビゲーションを `MonthNav` と `useSelectedMonth` に切り出し、ホームと履歴で共用する

### 修正
- カレンダーのスワイプ領域の `touch-action` を `none` から `pan-y` に変更。ページに埋め込んだときに縦スクロールが止まるのを防ぐ

### 技術的変更
- `SettingsHeader` / `ReviewHeader` を削除（タブがあるので戻る矢印が不要になったため）
- Service Worker のプリキャッシュに `/history` を追加し、`CACHE_NAME` を `v4` に更新

## [5.4.0] - 2026-08-29

### 追加
- **デプロイ中のコミットSHAを表示**: 画面下部のバージョン表記の隣に、Vercel にデプロイされているコミットの先頭7桁を出す。「デプロイしたつもりが本番は古いビルドのままだった」を画面から判別できるようにするため。Vercel のシステム環境変数 `NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA` を使い、ローカルでは何も表示しない

## [5.3.0] - 2026-08-29

### 追加
- **年間振り返り画面 (/review)**: 年をまたいで見返すための新しい画面。ダッシュボードのヘッダー（モバイルは三点メニュー）と「今月の収支」カードから開ける。PWA のショートカットにも追加
  - **年収の推移**: 手取り + 社会保険料 + 所得税 + 住民税 の積み上げ棒。棒全体の高さが額面になる
  - **支出と投資の推移**: 支出と投資の積み上げ棒に手取り収入の線を重ね、手元に残った分が読めるようにした
  - **貯蓄率の推移** と **累計投資額（元本）の推移**
  - 年を選ぶと **手取りの使い道**（支出/投資/残りの内訳）、**カテゴリ別支出の前年比**、**月別内訳** を表示
  - 「年収の推移」「支出と投資の推移」は、棒の上に合計を表示し、グラフの下に内訳の金額テーブルを置く（グラフからは金額そのものが読み取れないため）。凡例と同じ色見本を見出しに付けて系列と対応させる
- **額面年収の推定**: 記録している手取りから、社会保険料・所得税・住民税を年分ごとの料率で計算して額面を逆算する。料率と控除額は `src/utils/tax/rates.ts` に集約し、2024年以前 / 2025年 / 2026年の3世代に対応（2025年・2026年の「年収の壁」改正で基礎控除と給与所得控除が大きく変わったため、年を無視すると数十万円ずれる）
- **年次集計のユーティリティ**: 各コンポーネントに散っていた年集計を `src/utils/annualSummary.ts` の純関数に集約

### 改善
- **年間収支モーダルを廃止し、年間振り返り画面に統合**: このモーダルだけ「年間支出に投資を含める」という他と食い違う定義を持っていた。新画面では支出（投資除く）と投資額を常に別々に表示する

### 技術的変更
- グラフの系列色を `globals.css` のトークン（`--series-*`）として追加。意味が固定された系列（社会保険料・所得税など）はユーザーのカテゴリ色と混ざらないようにした
- 年間振り返りの全グラフで Recharts のアニメーションを無効化。データの件数が変わる再描画（読み込み中の空配列 → 取引到着）で補間が破綻し、棒や面が描画されないままになるため

## [5.2.0] - 2026-08-17

### 追加
- **Firestore セキュリティルールをリポジトリで管理**: `firestore.rules` を追加し、`firebase.json` から参照する。取引（トップレベル `transactions`）は自分の `userId` のドキュメントのみ読み書き可、所有者の付け替えを禁止、未定義パスは全拒否 (#65)
- **セキュリティヘッダー**: `X-Frame-Options` / `X-Content-Type-Options` / `Referrer-Policy` / `Permissions-Policy` / `HSTS` を全ページに付与。CSP は Report-Only で導入（動作確認後に強制へ切り替える） (#68)
- **Firebase App Check の任意対応**: `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` を設定すると有効になる。未設定時は初期化しない (#69)
- **CSV インポートの上限と検証**: ファイル 2MB・5000行の上限、日付/金額/種別/カテゴリの検証、メモ200文字への切り詰め。スキップした行数と理由、設定に無いカテゴリを結果に表示する (#70)
- **Dependabot**: npm の更新を毎週チェックする (#67)

### 改善
- CSV インポートを 500 件ずつの `writeBatch` に変更（1件ずつの書き込みをやめ、往復回数を削減） (#70)
- 新規登録時のパスワードを 10 文字以上に引き上げ（既存アカウントのログインは従来どおり） (#66)

### 修正
- **CSV エクスポートの数式インジェクション対策**: `=` `+` `-` `@` で始まる値の前に `'` を付ける。インポート時に元へ戻すため往復しても値は変わらない (#71)
- **ログイン画面のアカウント列挙**: 「未登録」と「パスワード違い」でメッセージを分けていたのを統一 (#66)
- **本番ビルドでの情報漏れ**: 取引データそのものをコンソールに出力していた箇所を修正し、本番では `console.error` 以外を除去 (#72)
- **Service Worker のキャッシュ範囲**: 自オリジンの正常応答のみをキャッシュするよう変更（ドメイン名の文字列一致による判定をやめた） (#74)
- `next.config.ts` の未使用 `env: { CUSTOM_KEY }` を削除（秘密情報がクライアントへ混入する入口） (#73)

### 技術的変更
- 依存関係の既知の脆弱性 17 件（critical 3 / high 11）を解消。Next.js を 16.3.1 に更新 (#67)
- `docs/deployment.md` / `AGENTS.md` のセキュリティ記述を実装に合わせて更新

## [5.1.0] - 2026-08-16

### 追加
- **開発元表記**: 画面下部のバージョン表示に `© Gorillaburg Inc.` を追加

### 修正
- **カテゴリ別支出推移グラフ**: 比較カテゴリの選択がリロードで既定（支出Top3）に戻る不具合を修正。選択を localStorage に保存する `usePersistedState` フックを追加し、保存後に改名・削除されたカテゴリは表示時に除外する

### 技術的変更
- 変更ごとにバージョンと変更履歴を更新する運用を `AGENTS.md` に明記
- `docs/CHANGELOG.md` の記載が 1.0.0 で止まっていたため、1.1.0 〜 5.0.0 を履歴から補完

## [5.0.0] - 2026-07-03

### 追加
- **設定ページ (/settings)**: カテゴリ・支払方法・月間予算をアプリ上から編集可能に
- **ユーザー設定のデータモデル**: Firestore の `users/{uid}/settings/app` と `SettingsContext` を追加
- **月次支出ペースグラフ**: 週別累計の積み上げ棒グラフ + 理想ペースライン

### 改善
- **UI 全面刷新**: デザインシステム "Quiet Ledger"（フラットな面 + ヘアライン境界）を導入
- **集計ロジックを役割ベースへ変更**: カテゴリ名の文字列一致をやめ `CategoryRole` で判定するようにし、カテゴリを自由に変更しても集計が壊れないように
- PWA の最適化と円グラフの UI 改善
- 旧ハードコード定数を削除しドキュメントを整備

### 修正
- ダークモードで収支の意味色が白飛びする問題
- スマホ入力時に金額欄で数字が重複する不具合
- 前月比の計算バグ、CSV 入出力の不具合
- Textarea のクラッシュ、ログイン失敗時に dev オーバーレイが出る問題
- 月次支出ペースから家賃・投資（定期課金）を除外

## [2.0.0] - 2025-12-28

### 追加
- **立替金カテゴリ**: 年間集計から除外する仕組みを追加
- **カテゴリ別支出推移グラフ**: 支出Top3を自動選択して比較表示

### 改善
- 貯蓄率の算出にボーナスを含めるよう変更し、年間収支に注記を追加
- ダッシュボードの余白を詰めた高密度レイアウト
- モバイルの円グラフを大型化（半径 105px）
- カレンダーのグリッド線を細く均一に

### 修正
- 月次収支を発生主義（収入 − 支出）に統一
- ツールチップの NaN% 表示
- 定期取引の重複判定を名称・説明の一致で厳密化
- 投資の注記を年間支出側へ移動

## [1.1.0] - 2025-12-27

### 追加
- モバイル UI を Bento Grid レイアウトへ刷新
- 貯蓄率・貯蓄額の表示とグラフ改善
- ダークモード対応と年月移動 UI の改善

### 技術的変更
- Next.js を 16.1.1 に更新（CVE-2025-66478 対応）
- ESLint / ビルドエラーの整理

## [1.0.0] - 2025-10-26

### 追加
- **バージョン表示機能**: アプリ下部にバージョン情報を表示
- **VersionDisplayコンポーネント**: バージョン情報を表示する新しいコンポーネント

### 改善
- **年月移動のパフォーマンス**: 月選択時のスクロール無効化で反応速度を向上
- **取引フォームのモバイル最適化**:
  - **NativeSelectの採用**: モバイルではネイティブselect要素を使用し、スクロール問題を根本解決
  - デスクトップでは従来のSelectコンポーネントを維持（検索機能付き）
  - フォントサイズを16pxに統一（ズーム防止）
  - タップ領域を48pxに拡大
- **取引フォームのUI改善**:
  - **収入入力時に支払方法を非表示**: 収入には支払方法が不要なため、フォームをシンプル化
  - 収入⇔支出切り替え時に支払方法を自動クリア
- **カテゴリ構造の最適化**:
  - **光熱費カテゴリを削除**: 電気代、ガス代、水道代を固定費のサブカテゴリに統合
  - より直感的なカテゴリ分類
- **SegmentedControlの最適化**: モバイルでのタップ領域を拡大
- **Textareaの改善**: 自動サイズ調整とモバイル最適化
- **カレンダーのタップ反応改善**:
  - タップハイライトの最適化
  - トランジション速度の高速化（0.15s）
  - touchActionの追加で誤タップを防止
- **グローバルCSS改善**:
  - ボタンとクリック可能要素のタップ反応を改善
  - Selectドロップダウンのタップ反応を最適化

### 修正
- **モバイルPWAでの入力問題**: タッチ操作の安定性を向上
- **Selectコンポーネントの不安定性**: NativeSelectの採用で長いリストのスクロール問題を解決
- **Textareaのエラー**: minHeightスタイルを削除し、minRowsプロパティのみ使用

### 技術的変更
- Next.js: 15.3.4 → 15.5.6（セキュリティ修正）
- TypeScript target: ES2017 → ES2020
- ESLint設定の強化
- 環境変数管理の改善

## [0.1.0] - 初期リリース

### 機能
- 収支管理
- カテゴリ別集計
- グラフ表示
- CSV インポート/エクスポート
- テンプレート機能
- カレンダー表示
- カード還元ポイント計算
- Firebase認証とデータ保存
